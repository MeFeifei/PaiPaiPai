<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_index"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.feifei.paipaipai.Index"
    tools:showIn="@layout/activity_index">

    <include layout="@layout/head"
        android:id="@+id/top_bar"/>

    <LinearLayout
        android:id="@+id/tab_bar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:background="#fff"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/txt_video"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/bottom_bar_selector"
            android:drawablePadding="3dp"
            android:drawableTop="@drawable/icon_video_selector"
            android:gravity="center"
             />

        <TextView
            android:id="@+id/txt_record"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/bottom_bar_selector"
            android:drawablePadding="3dp"
            android:drawableTop="@drawable/icon_record_selector"
            android:gravity="center"
             />

        <TextView
            android:id="@+id/txt_info"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/bottom_bar_selector"
            android:drawablePadding="3dp"
            android:drawableTop="@drawable/icon_info_selector"
            android:gravity="center"
             />


    </LinearLayout>

    <View
        android:id="@+id/div_tab_bar"
        android:layout_width="match_parent"
        android:layout_height="2px"
        android:background="#fff"
        android:layout_above="@id/tab_bar"/>


    <FrameLayout
        android:id="@+id/home_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/top_bar"
        android:layout_above="@id/div_tab_bar"
        >

    </FrameLayout>


</RelativeLayout>
